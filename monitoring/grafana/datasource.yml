# Grafana datasource provisioning — Prometheus на localhost (приватный контур).
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090   # localhost-only
    isDefault: true
    editable: false
