# Core
openai>=1.0.0
httpx>=0.24.0
numpy>=1.24.0
pandas>=2.0.0

# Voice
faster-whisper>=0.9.0
kokoro>=0.7.0
soundfile>=0.12.0

# OSINT (spiderfoot installed standalone, not via pip)

# System
psutil>=5.9.0
structlog>=23.0.0
aiofiles>=23.0.0
aiohttp>=3.8.0

# Sprint 2 — Remizov solver + multimodal pipeline
scipy>=1.11.0     # RemizovSolver: odeint, quad
sympy>=1.12.0     # Symbolic math (optional)
pillow>=10.0.0    # Image modality in MultimodalContextManager
librosa>=0.10.0   # Audio feature extraction for voice modality

# Sprint 3 — Qwen3-Omni + Deep Hedging + Signature Methods
transformers>=4.30.0  # Qwen3OmniIntegration: AutoProcessor, Qwen2AudioForConditionalGeneration
torch>=2.0.0          # DeepHedgingNetwork (nn.Module)
torchaudio>=2.0.0     # Audio tensor ops (torchaudio.load / resample)
torchvision>=0.15.0   # Vision tensor ops (optional multimodal path)
# iisignature>=0.2.3  # optional: Chen iterated integrals; falls back to np.diff if absent
#   to install manually: pip install --no-build-isolation iisignature
tqdm>=4.65.0          # Training progress bars (DeepHedgingFramework.train_hedging_strategy)

# Sprint 4 — Mean Field Games + Performance Optimizer
scikit-learn>=1.3.0   # MeanFieldGames: optional clustering/regression helpers
matplotlib>=3.7.0     # Visualisation of MFG trajectories (optional)
threadpoolctl>=3.1.0  # ThreadPoolExecutor control (PerformanceOptimizer)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
